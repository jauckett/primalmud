#define PORT 	"4001"
#define QUEUE	5
#define NO_CONNECT 0
#define SERVER			1
#define CLIENT			2
#define FALSE 0
#define TRUE  1

#define SERVER_HOSTNAME	"mud.alphalink.com.au"

#define MAX(x, y)	((x) > (y) ? (x) : (y))
