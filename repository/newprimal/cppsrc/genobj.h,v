head	1.2;
access;
symbols
	A3_0_59:1.1
	LATEST:1.1
	A3_0_1:1.1;
locks; strict;
comment	@ * @;


1.2
date	2001.08.11.14.04.56;	author artus;	state Exp;
branches;
next	1.1;

1.1
date	2000.05.12.07.43.58;	author mbd;	state Exp;
branches;
next	;


desc
@@


1.2
log
@Modified: DM

Changed:
- made the following functions take another argument (the virtual zone number)
  to apply to the virtual zone attribute in the obj_index array of protos.
  Set when a char modifies/creates an object and used to determine which virtual
  zone the object belongs to (for olc users loading objects etc)

index_object
insert_object
add_xxxx
@
text
@/************************************************************************
 * Generic OLC Library - Objects / genobj.h			v1.0	*
 * Original author: Levork						*
 * Copyright 1996 by Harvey Gilpin					*
 * Copyright 1997-1999 by George Greer (greerga@@circlemud.org)		*
 ************************************************************************/

void copy_object_strings(struct obj_data *to, struct obj_data *from);
void free_object_strings(struct obj_data *obj);
void free_object_strings_proto(struct obj_data *obj);
int copy_object(struct obj_data *to, struct obj_data *from);
int copy_object_preserve(struct obj_data *to, struct obj_data *from);
int save_objects(zone_rnum vznum);
int insert_object(struct obj_data *obj, obj_vnum ovnum, zone_vnum vznum);
int adjust_objects(obj_rnum refpt);
int index_object(struct obj_data *obj, obj_vnum ovnum, obj_rnum ornum, zone_vnum vznum);
int update_objects(struct obj_data *refobj);
obj_rnum add_object(struct obj_data *, obj_vnum ovnum, zone_vnum vznum);
@


1.1
log
@

Added:
- Modified files to compile with g++ - this is the initial import of those
  files.

TODO:
- Test it all works 100%
@
text
@d14 1
a14 1
int insert_object(struct obj_data *obj, obj_vnum ovnum);
d16 1
a16 1
int index_object(struct obj_data *obj, obj_vnum ovnum, obj_rnum ornum);
d18 1
a18 1
obj_rnum add_object(struct obj_data *, obj_vnum ovnum);
@

